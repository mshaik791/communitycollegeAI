{
  "name": "communitycollegeAI",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for student retention platform (web + api)",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "prisma:generate": "pnpm --filter api prisma:generate",
    "prisma:migrate": "pnpm --filter api prisma:migrate",
    "prisma:studio": "pnpm --filter api prisma:studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.17.1",
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {},
  "pnpm": {
    "packageExtensions": {}
  }
}
